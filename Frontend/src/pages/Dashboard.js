// src/pages/Dashboard.js
import React from 'react';
import { Container, Typography } from '@mui/material';

const Dashboard = () => (
  <Container>
    <Typography variant="h4" mt={2}>Welcome to Factory UI</Typography>
    <Typography>Use the menu to manage inventory and production orders.</Typography>
  </Container>
);

export default Dashboard;
